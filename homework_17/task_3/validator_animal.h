#include <string>

using namespace std;

namespace validatorAnimal
{
    enum AgeAnimal
    {
        Puma = 3,
        Bars = 9,
        Cherk = 19,
        Default = 0,
    };
    string correctAlpha(string str);
    int correctCount(int count);
    void delimiter();
};